---
import carpeta from "../../../assets/carpeta.webp";

const { opacity } = Astro.props;
---

<img
  src={carpeta.src}
  alt="Imagen de carpeta"
  class={`${opacity} drop-shadow-[0_0_10px_rgba(0,0,0,0.25)]`}
/>
